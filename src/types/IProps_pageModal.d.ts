export interface IProps {
  modalConfig: {
    pageName: string
    id: string
    header: {
      newTitle: string
      editTitle: string
    }
    formItems: any[]
  }
  otherInfo?: any
}

// props数据类型
